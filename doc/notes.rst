
It has a symbolic language which allows to express symbolic formulas.
But it is also extensible.

The loss functions are categorized as

Logistic loss is:
   * Finite sum
   * Smooth

Hinge loss is:
   * Finite sum
   * Proximable, can compute fenchel dual.
